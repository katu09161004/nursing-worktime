# さくらVPS モバイル入力環境 構築手順

DL380で動かしているモバイル記録環境を、さくらVPS上に **HTTPS + ログイン認証付き** で構築する手順。
スマホ・タブレットのブラウザから記録でき、「ホーム画面に追加」でアプリのように使える。

---

## 先に確認しておくこと

### 1) データの置き場所として妥当か
記録するのはPHIではないが、**病棟名・勤務帯・業務時間という病院の運営情報**ではある。
インターネット上のVPSに実データを置く判断は、施設のポリシーに照らして事前に整理しておくこと。
迷う場合は、`config_local.py` の `WARDS` を汎用値（病棟A/B/C）のままにして、
どの病棟か分からない形で運用する手もある。

### 2) 準備するもの
- さくらVPS（Ubuntu 22.04 / 24.04、一般ユーザーでssh可能）
- **ドメイン**（Aレコードを VPS のグローバルIPに向けておく）
  → HTTPSは「ホーム画面に追加」やオフライン動作に必須なので省略できない
- **さくらVPSコントロールパネルのパケットフィルタで 22 / 80 / 443 を許可**
- 決めておく値：スタッフ共有ログインパスワード、PostgreSQLパスワード、Let's Encrypt用メール

---

## 構築（VPS上で1コマンド）

```bash
ssh <ユーザー>@<VPSのIP>
git clone https://github.com/katu09161004/nursing-worktime.git ~/nwt-src
sudo bash ~/nwt-src/deploy/setup-mobile.sh
```

対話でドメイン・実行ユーザー・ログインパスワード・DBパスワード・メールを入力すると、以下が自動で行われる。

1. PostgreSQL に `worktime` DB を作成
2. `/opt/nwt-mobile` にアプリ配置、venv 構築
3. `config_local.py` 生成（DB接続 + 認証設定。**Excel用APIキーとセッション鍵は自動生成**）
4. systemd 常駐（**127.0.0.1 で待受**。外部からはNginx経由のみ）
5. Nginx リバースプロキシ
6. ufw で 22/80/443 のみ許可
7. Let's Encrypt で HTTPS 化（httpへのアクセスは https にリダイレクト）
8. `pg_dump` の日次バックアップ（14世代）を cron に登録

完了時に **Excel用APIキー** が表示されるので控えておくこと。

---

## スマホでの使い方（スタッフへの案内）

1. ブラウザで `https://<ドメイン>/` を開く
2. **共有パスワードでログイン**（一度ログインすれば30日間は再入力不要）
3. 担当者・病棟を選んで「この設定で記録を始める」
4. 業務のボタンをタップして記録
5. （任意）ブラウザメニューから **「ホーム画面に追加」** → アイコンからアプリのように起動できる

集計は `https://<ドメイン>/dashboard`。

---

## Excel から送る場合

入力シート右の「サーバ同期設定」に以下を設定する。

| セル | 内容 |
|---|---|
| N3 | `https://<ドメイン>` |
| N4 | `ON` |
| **N7** | **セットアップ完了時に表示されたAPIキー** |

APIキーが未設定・誤りだと送信時に「認証に失敗しました（401）」と表示される。
※Excel同期はWindows専用（Mac版Excelでは記録マクロのみ動作）。

---

## 動作確認

```bash
# ログイン画面が出る（302 → /login）
curl -o /dev/null -w "%{http_code}\n" https://<ドメイン>/

# 認証なしのAPIは弾かれる（401 が正しい）
curl -o /dev/null -w "%{http_code}\n" https://<ドメイン>/api/summary

# アプリに直接アクセスできない（接続できないのが正しい）
curl --max-time 5 http://<VPSのグローバルIP>:8300/health
```

スマホの実機で開き、ログイン → 記録 → ダッシュボードに反映、まで確認する。

---

## 運用

```bash
# 更新
cd /opt/nwt-mobile && sudo git pull
sudo systemctl restart nursing-worktime

# ログ
sudo journalctl -u nursing-worktime -f

# 病棟名・スタッフIDの変更
sudo nano /opt/nwt-mobile/config_local.py   # 編集後 restart

# パスワード／APIキーの変更
sudo nano /opt/nwt-mobile/config_local.py   # AUTH の password / api_key を変更 → restart
#   ※ secret を変えると全員ログインし直しになる

# バックアップ確認
ls -la /var/backups/nwt/
```

---

## セキュリティ設計（何がどう守られているか）

| 項目 | 実装 |
|---|---|
| 通信 | Let's Encrypt による HTTPS（httpは自動リダイレクト） |
| 画面アクセス | 共有パスワードによるログイン。署名付きCookie（HMAC-SHA256、30日） |
| API（Excel） | `X-API-Key` ヘッダ。キーはセットアップ時に自動生成 |
| 認証不要 | `/health`、`/login`、`/manifest.json`、`/icons/*`（PWA表示に必要な最小限） |
| アプリの待受 | `127.0.0.1` のみ。8300番はインターネットに公開しない |
| ファイアウォール | ufw + さくらパケットフィルタで 22/80/443 のみ |
| 検索避け | `robots.txt` で全拒否 |
| 秘密情報 | `config_local.py`（600、`.gitignore`済み）。**コミット禁止** |

**注意**：認証は「スタッフ共有パスワード」方式で、個人単位の認証ではない。
誰が記録したかは画面で選ぶ匿名ID（N-01等）で区別する。パスワードが外部に漏れると
第三者がアクセスできるため、退職者が出た場合などは `AUTH.password` を変更すること。

より厳格にしたい場合は、公開せず **Tailscale経由のみ**にする構成（`HANDOFF-vps.md` タスクC）を選ぶ。

---

## 院内DL380との関係

同じコードなので、両方に同じ構成を置ける。使い分けの例：

- **DL380（院内）**：オーダー端末・病棟タブレットからの本番記録（LAN内、認証なしでも可）
- **VPS**：院外からの記録・出先での確認・デモ

ただし**DBは別**なので、2か所に記録すると集計が分かれる。**どちらか一方を正とする**こと。
両方のデータを合算したい場合は、`pg_dump` で移すか、記録先をVPSに一本化する。
